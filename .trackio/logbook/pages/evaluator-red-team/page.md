# Evaluator-blind red-team record

The reviewer started from `logbook.json` and its declared root
`pages/current-index.md`; no repository knowledge or OpenResearch run database
was supplied.

## Pass 1

Opened: current index; Claims 1–5 current pages; visibility matrix; release
report; raw links declared by those pages.

Unverifiable before fixes:

- raw Claim 1–3 links pointed outside the Space layout;
- Claim 5 had no current canonical page;
- no single page exposed the release forecast and visibility matrix.

Fixes: direct public raw links, a current Claim 5 page, and current visibility
and release pages were added to root navigation.

## Pass 2

Opened the same root-derived file set after fixes. Every claim exposed the
exact contract and verdict, fixed command, current code/checker, inline result,
raw link, control, limitations, Git/seed/CPU/runtime information where
applicable. No hidden repository path was needed for a conclusion.

Conclusions:

- Claims 1–4 are discoverably BLOCKED; no historical toy verifier appears as
  current.
- Claim 5 is discoverably VERIFIED.
- The visible forecast remains 6/10 and is labeled as a forecast.

The executable traversal record is generated by `repro/run_release_audit.py`.

## Pass 3 — fresh staged candidate

A fresh empty directory was populated from exact judged revision
`f360979669367144cd429766b5952338e263d3d9`, then only the committed upload
allowlist from candidate `0c9f970126a6ea082382feaece0d2f969d3ea557`
was applied. Starting again from `logbook.json` and
`pages/current-index.md`, the reviewer opened the 12 navigation files listed
above, resolved 15 raw/code links within the staged tree, and found:

- 65/65 manifest payload hashes valid;
- 37/37 historical paths retained, with no unapproved historical hash change;
- zero missing navigation files, raw targets, visibility cells, or secret hits.

This pass did not consult OpenResearch logs or unpublished paths to fill any
claim conclusion.
