# Claim 3 limitations

The existing truth-table checks establish only finite composition behavior.
They do not establish FO-uniformity, completeness reductions, the lower bound
over all languages, or every `d>=1`. No valid counterexample was found.
Verdict: `BLOCKED`.
